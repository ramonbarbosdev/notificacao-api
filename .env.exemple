
SPRING_PROFILES_ACTIVE=dev
PORT=8086

# Banco (obrigatorio)
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=db_notificacao
DB_USERNAME=ramon
DB_PASSWORD=admin
DB_SSLMODE=disable

# JWT (obrigatorio em producao)
JWT_SECRET=notificacao-api-dev-secret-key-32-characters-minimum
JWT_EXPIRATION_MINUTES=120

# APP_SWAGGER_SERVER_URL=http://localhost:8086/api

# WhatsApp Gateway
WHATSAPP_GATEWAY_BASE_URL=http://localhost:3001
WHATSAPP_GATEWAY_API_KEY=dev-secret-123

# Criptografia de credenciais WhatsApp (obrigatorio se usar Meta Cloud / tokens no banco)
# WHATSAPP_CREDENTIAL_ENCRYPTION_KEY=

# Meta Cloud API (obrigatorio para Embedded Signup / webhook)
META_APP_ID=
META_APP_SECRET=
META_EMBEDDED_SIGNUP_CONFIG_ID=
META_OAUTH_REDIRECT_URI=https://notificacao.ramoncode.com.br/whatsapp-cloud/callback
META_WEBHOOK_VERIFY_TOKEN=
WHATSAPP_CREDENTIAL_ENCRYPTION_KEY=


